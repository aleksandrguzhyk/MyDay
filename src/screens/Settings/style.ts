import {StyleSheet} from 'react-native'
import {lightGreyBGColor} from '../../theme'

export const styles = StyleSheet.create({
  container: {
    backgroundColor: lightGreyBGColor,
    paddingHorizontal: 20,
  },
})
